package abstraction;

// ! Interface
// SKill (技能) or Contract (合約)
public interface Flyable {
  // ! Interface has NO attributes.
  // ! Interface has NO concrete method. (public & abstract only)
  // Interface is 100% abstraction (Before Java 8).
  void fly(); // !implicityly public
}
